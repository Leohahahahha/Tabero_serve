# Tabero-VTLA 问题排查与面试复盘

本文持续记录 Tabero-VTLA 从数据采集、训练、离线评估到 FR3 真机部署过程中遇到的问题。
日期表示问题被定位或形成可靠结论的日期，不一定是故障首次发生的日期。

状态定义：

- **已解决**：根因明确，修复及针对性验证已完成。
- **已缓解**：已有防护或绕过方案，但上游根因仍存在。
- **待验证**：实现已经完成，还缺训练、离线评估或真机实验验证。
- **根因未定**：证据不足，只能保留候选原因。

## 问题总览

| 日期 | 问题名称 | 分类 | 状态 |
|---|---|---|---|
| 2026-09-02 | Bash 多行粘贴触发 `free(): invalid pointer` | 工程环境 | 已缓解 |
| 2026-09-02 | Python 环境安装与项目打包连续失败 | 工程环境 | 已解决 |
| 2026-09-02 | LeRobot 非连续 episode 子集索引错误 | 数据加载 | 已解决 |
| 2026-09-03 | 训练约 260 步出现 NaN 且没有可恢复 checkpoint | 训练稳定性 | 根因未定、已加防护 |
| 2026-09-03 | v1 跨 episode 继承旧动作目标 | 数据标签 | v3 已解决 |
| 2026-09-03 / 2026-09-09 | 轴角在 ±π 附近产生约 2π 数值跳变 | 动作表示 | v3 训练路径已解决、待训练验证 |
| 2026-09-03 | 真机动作单位和坐标语义容易重复变换 | 接口契约 | 已解决 |
| 2026-09-07 | 旧离线测试使用全零触觉样本 | 测试数据 | 已定位 |
| 2026-09-07 | 触觉与 RGB 基线不是严格单变量对照 | 实验设计 | 已识别、待严格实验 |
| 2026-09-07 | 最低验证 loss 不等于最佳物理动作指标 | 模型选择 | 已解决方法问题 |
| 2026-09-08 | 触觉桥未启动导致 `stopped: "'left'"` | 现场启动 | 已解决 |
| 2026-09-08 | checkpoint、norm stats 与 conversion 可能配错 | 模型资产 | 本次已排除、已加校验 |
| 2026-09-08 / 2026-09-09 | Shadow 中异步索引放大动作与当前状态距离 | 时序控制 | 已改同步、待 shadow 验证 |
| 2026-09-08 | 相同观测下不同 action chunk 波动和切换跳变 | 策略稳定性 | 待解决 |
| 2026-09-08 / 2026-09-09 | 夹爪轻微越界掩盖位置和姿态错误 | 安全检查 | 已改显式饱和、待 shadow 验证 |
| 2026-09-08 | 部署 prompt 与训练任务文本不一致 | 输入契约 | 已解决 |
| 2026-09-08 / 2026-09-10 | 真机起始姿态超出训练分布 | 分布偏移 | 旧运行已确认；本次五个 K=2 起点已对齐 |
| 2026-09-08 | 传感器时间偏差和旧 chunk 生命周期诊断不足 | 时序诊断 | 已改善、部分待修 |
| 2026-09-09 | v3 压缩缺帧与部署限速不匹配 | 数据时序/控制 | 待解决 |
| 2026-09-09 | Tokenizer 测试默认缓存目录只读 | 工程环境 | 已解决 |
| 2026-09-09 | v3 4000/20000 checkpoint 指标冲突 | 模型选择 | 离线选择完成、待多 seed 与 shadow 验证 |
| 2026-09-09 | 预测饱和、同步推理与 chunk 步数协调 | 部署控制 | K=1 首次真机已运行、闭环质量待验证 |
| 2026-09-09 | v1 服务配置加载 v3 checkpoint 20000 | 模型资产 | 已解决并经推理主机验证 |
| 2026-09-09 | v3-20000 同步 shadow 的时延、限幅与随机波动 | 部署评估 | 基础门限通过，起始姿态与波动待处理 |
| 2026-09-09 | 首次同步 execute 接近 workspace 下界 | 真机执行 | 已安全完成，暂停重复执行并待姿态复位 |
| 2026-09-10 | 长时同步 execute 因 ZED 前视帧延迟触发传感器 skew 停止 | 传感器时序/真机安全 | 已安全停止，前视帧延迟根因待定位 |
| 2026-09-10 | 同步 K=1 控制吞吐受推理延迟限制 | 部署控制 | K=2 控制稳定但五次任务失败；K=5不满足时效门限 |
| 2026-09-10 | 离线动作误差小但五次 K=2 真机任务失败 | 闭环评估/策略部署 | 已定位指标差异和闭环漂移，最终任务根因待分层验证 |
| 2026-09-10 | v3 checkpoint 与推理主机旧代码的 SO(3) 契约漂移 | 部署配置/动作表示 | 本机已排除；推理主机版本待确认，禁止执行 |
| 2026-09-15 | 白板 next-state 与 sent-command 配对训练 | 数据标签/实验设计 | next-state已从8k恢复续训；新12k提交待验证 |
| 2026-09-15 | 白板训练遗漏 `wrist_wrench` 力/力矩监督 | 训练目标/部署契约 | 13D训练路径已实现；训练、力指标和真机效果待验证 |
| 2026-09-16 | 白板 checkpoint 异步提交失败与 W&B crashed | 训练可靠性/Checkpoint | 竞态修复已实测提交12k；sent-command训练中，续训W&B步号回退待改进 |
| 2026-09-16 | 单条 LeRobot 样本直接打印导致大数组淹没终端 | 数据检查工具 | 已解决查看方法 |
| 2026-09-16 | 两卡白板全参数微调的显存与初始化契约 | 训练配置/显存 | AdamW首步OOM；无状态SGD真实运行至step 990但无明确收敛趋势 |
| 2026-09-17 | 全量SGD早期平台期、三卡AdamW分片与GPU驱动失联 | 优化器/显存/训练可靠性 | SGD收敛结论待更多验证；三卡AdamW高风险；本次运行因GPU驱动失联中止 |
| 2026-09-17 | full-SGD sent-command 在step-0验证产生NaN | 数值稳定性/训练可靠性 | 直接失败点已定位；数据输入已排除，GPU/模型前向根因待复现 |
| 2026-09-17 | 同规模pi0.5双卡DDP仅33 GiB与本项目AdamW OOM差异 | 精度/优化器/显存 | 已定位为内存布局差异；同门精确dtype与统计时点待确认 |
| 2026-09-17 | 两卡FSDP混合精度全量AdamW策略 | 精度/优化器/显存 | FP32梯度/Adam状态主档及低显存备选已实现；本项目真实首步待验证 |
| 2026-09-17 | 四套白板12000步权重缺少13D离线评估入口 | 离线评估/模型选择 | 评估实现与CPU合同预检完成；GPU推理待驱动恢复 |
| 2026-09-17 | 顺序启动器预处理阶段尚无单模型训练日志 | 训练可观测性 | 已定位，使用pair总日志或`tail -F`等待训练日志创建 |

## 1. Bash 多行粘贴触发 `free(): invalid pointer`

- **日期**：2026-09-02
- **状态**：已缓解，上游注入库仍需维护者修复。
- **现象**：在 VS Code 终端粘贴带反斜杠换行的命令时，Bash 直接 SIGABRT；单行命令正常。
- **排查过程**：先排除 Python、JAX 和 VS Code；在独立交互式 Bash 中用一条 `echo` 加换行即可稳定复现。分析 core dump 后，调用栈落在 `record_history_cmdline -> record_cmdline_core -> free`。
- **根因**：`/usr/lib/libpayload.so` 的命令记录注入逻辑把只读区的回退字符串 `"-"` 当作堆内存释放。它不是项目代码或 Python 依赖错误。
- **解决方案**：所有操作命令保持单行；复杂流程写入 shell 脚本后只执行一条脚本命令；向系统维护者提交最小复现、core stack 和库路径。
- **验证**：单行和脚本文件执行正常；交互式反斜杠续行持续稳定复现崩溃。
- **面试讲法**：强调“逐层缩小故障边界”。先从应用层降到空 Bash，再用 core dump 把问题定位到本机注入库，避免错误地重装深度学习环境。

## 2. Python 环境安装与项目打包连续失败

- **日期**：2026-09-02
- **状态**：已解决。
- **现象**：冻结依赖安装先后遇到 lockfile 与项目名不一致、Hatch 无法自动发现包、PyAV 14.4.0 尝试源码构建并缺少 FFmpeg 开发头文件。
- **根因**：项目被重命名后打包元数据没有同步；源码布局为 `src/openpi`，需要显式配置；当前平台没有 PyAV 14.4.0 的合适安装路径，而 14.2.0 有兼容 wheel。
- **解决方案**：修正项目打包配置，显式指定 `src/openpi`；使用兼容 LeRobot 的 PyAV 14.2.0 wheel；在 `/data` 建立隔离 Python 3.11 环境。
- **验证**：225 个依赖安装完成；Python 3.11.16、JAX 0.5.3、PyTorch 2.7.1、LeRobot 0.3.3 和 PyAV 14.2.0 可导入，选定 CPU 测试通过。
- **面试讲法**：依赖问题需要区分解析、打包和本地编译三个阶段，每个阶段用对应证据处理，不靠反复删除环境碰运气。

## 3. LeRobot 非连续 episode 子集索引错误

- **日期**：2026-09-02
- **状态**：已解决。
- **现象**：训练集和验证集按非连续 episode ID 划分，例如验证集 `(4,14,24)`；上游 LeRobot v0.3.3 的 episode 边界数组是子集相对索引，但查询时仍使用原始 episode ID，可能越界或跨 episode 取动作 chunk。
- **根因**：原始 ID 和子集位置混用。
- **解决方案**：增加本地数据集子类，将原始 episode ID 映射到子集位置后再查询边界；加载时裁剪不需要的大型列。
- **验证**：合成的非连续 episode 边界回归测试确认 action chunk 不跨轨迹；真实验证集长度和末尾 padding 与 Parquet 一致。
- **面试讲法**：强调数据管线中的“ID 语义”。shape 正确不代表样本属于正确轨迹，必须用边界断言测试。

## 4. 训练约 260 步出现 NaN 且没有可恢复 checkpoint

- **日期**：2026-09-03（故障运行开始于 2026-09-02）
- **状态**：根因未定；故障防护已完成。
- **现象**：`real_fr3_4gpu_3000_20260902_203541` 在 step 250 验证 loss 仍有限，step 251 日志正常，约 step 261 的 loss、grad norm 和param norm 同时成为 NaN。首次保存计划在 500 步，因此没有 checkpoint。
- **排查结论**：现有日志不能确定第一个坏 update，也不能证明是数据、梯度、优化器状态、混合精度或硬件导致。原训练循环在 host 检查前已经应用参数更新，发现 NaN 时健康状态可能已被覆盖。
- **解决方案**：在更新前后检查模型、梯度和优化器树；遇到非有限值拒绝该更新，保留更新前健康状态；保存失败 batch、首个坏叶子索引、`numerical_failure.json` 和可恢复 checkpoint，然后立即报错退出。
- **验证**：合成 NaN/Inf 测试确认坏更新不会写入健康状态；后续 3000 步恢复训练记录有限，但不能反推旧故障根因已经消失。
- **面试讲法**：明确区分“增强可观测性与可恢复性”和“证明根因”。没有原始坏 batch 时，不把猜测包装成结论。

## 5. v1 跨 episode 继承旧动作目标

- **日期**：2026-09-03；v3 于 2026-09-09 完成复核。
- **状态**：v3 数据标签已解决；采集端旧缓存的具体代码位置仍需公共采集文件确认。
- **现象**：离线曲线在 episode 开头出现 300～450 mm 动作跳变，真实 state 基本不动。
- **关键证据**：
  - v1 的 29 条轨迹中，28 条首帧 `action-state` 超过 50 mm；
  - 14 条首帧 action 七个维度完全等于上一轨迹的末帧 action；
  - episode 4 前 11 帧保持旧目标，位置差 423.359 mm，机器人 state 相对起点仅变化 0.0053 mm；
  - exporter 只是逐行转换原始 `action.pose7`，说明污染在原始采集 Parquet 中已经存在。
- **根因判断**：高度支持 episode reset 后动作缓存/心跳仍保留上一轨迹目标；缺公共 `record_episode` 源码，无法精确到采集端代码行。
- **解决方案**：v3 将监督明确定义为同一 episode 内 `actions[t] = state[t+1]`，并丢弃每条轨迹最后一个观测，彻底避免旧命令目标作为标签。
- **验证**：逐元素验证全部 29 条轨迹；v3 所有 action 都严格等于 v1 的下一 state。v1 有 527 帧位置差超过 50 mm、最大 456.515 mm；v3 为 0 帧、最大 45.501 mm。
- **面试讲法**：从图像异常回溯到 NPZ、Parquet、原始反馈和转换脚本，逐层证明跳变在哪一层首次出现；修复后用全量逐元素不变量验证，而不是只看几张曲线。

## 6. 轴角在 ±π 附近产生约 2π 数值跳变

- **日期**：2026-09-03 首次发现；2026-09-09 在 v3 全量量化并实现训练修复。
- **状态**：v3 训练路径已解决，等待新模型训练和评估。
- **现象**：连续两帧 rotvec 分量相差约 6.28 rad，看起来像末端旋转一圈；SO(3) 实际旋转通常不到 1°。
- **根因**：轴角表示在 180°附近不唯一。`+πu` 和 `-πu` 表示相同姿态，规范化转换会在分支两侧改变旋转轴符号。旧 `DeltaActions` 直接执行 `action_rotvec - state_rotvec`，把表示切换误当成真实旋转。
- **量化结果**：v3 有 114/6626 个一步标签发生分支切换，最大物理旋转仅 1.225°；旧分量相减却产生约 2π 残差。
- **解决方案**：v3 专用变换使用 `R_relative = inverse(R_state) * R_action`，模型输出后用 `R_action = R_state * R_relative` 恢复绝对姿态。位置仍用相对 XYZ，夹爪保持绝对单指坐标。
- **验证**：正反变换回归和 v3 CPU contract 测试通过；原始 Parquet 可保留轴角分支，因为进入训练前会转换为 SO(3) 最短相对旋转。
- **面试讲法**：不要把欧氏向量差直接用于非欧氏空间。姿态属于 SO(3)，损失和误差都必须尊重群结构。

## 7. 真机动作单位和坐标语义容易重复变换

- **日期**：2026-09-03
- **状态**：已解决并形成接口契约。
- **现象**：采集端包含 VR 增益、坐标映射和控制器目标；容易误以为部署时还要对模型输出再应用相同映射，或者把 7D action 当成七个关节角。
- **根因**：采集控制坐标和训练后策略输出坐标没有书面区分。
- **正确契约**：模型输出是绝对末端 `XYZ(m) + rotvec(rad) + 单指位置(m)`；HTTP `/pose` 使用 `XYZ + XYZW quaternion`；`/move_gripper` 使用总宽度，因此发送 `2 * finger`。策略逆变换已经恢复绝对动作，不能再次加当前 pose 或重复 VR 映射。
- **解决方案**：建立显式输入输出 adapter、shape/finite/unit 检查和 conversion hash；部署代码只做 rotvec→quaternion 与单指→总宽转换。
- **验证**：单位、四元数顺序、夹爪倍数和变换往返测试通过。
- **面试讲法**：机器人部署中最危险的错误常不是模型结构，而是坐标系、单位和“绝对/相对”语义不一致。

## 8. 旧离线测试使用全零触觉样本

- **日期**：2026-09-07
- **状态**：已定位为测试 fixture 问题；不代表真机或 checkpoint 不兼容。
- **现象**：分支专用部署测试 34 项通过，扩展测试 53 项通过、2 项失败。失败样本把 `[9,198,2]` 触觉坐标全部写成零。
- **根因**：真实触觉 contract 的第 0 帧必须是固定左右参考网格，后八帧是当前 marker 坐标。全零数组不是“无触觉”或“零剪切”，而是非法几何输入，被严格校验正确拒绝。
- **解决方案**：测试需要构造固定参考网格，或在明确测试无触觉模型时完全移除触觉分支；不能用全零坐标冒充无接触。
- **验证**：真机完整 shadow 日志中的左右触觉为有限、非零且 shape 正确。
- **面试讲法**：失败的测试不一定说明生产代码错，也可能说明 fixture 已落后于接口语义。判断标准是生产 contract，而不是让测试无条件变绿。

## 9. 触觉与 RGB 基线不是严格单变量对照

- **日期**：2026-09-07
- **状态**：已识别，严格实验仍待运行。
- **现象**：触觉模型和 RGB+state 模型的离线结果接近，部分指标反而无触觉更好，但两次运行初始化不同。
- **根因**：触觉模型从本地 tactile smoke 99 warm-start，RGB 模型从 published Tabero 49999 开始，不能把差异只归因于触觉模态。
- **解决方案**：固定数据划分、初始化、训练步数、随机种子、归一化、离线 anchor 和采样噪声，只改变触觉输入/分支；按 episode、接触阶段和任务成功率报告。
- **验证结论**：现有证据不能证明触觉有效，也不能证明触觉无用。离线逐帧误差也不能替代真机任务成功率。
- **面试讲法**：机器学习实验必须控制变量。看到指标差异先审计实验配对关系，再讨论模型结论。

## 10. 最低验证 loss 不等于最佳物理动作指标

- **日期**：2026-09-07
- **状态**：模型选择方法已修正。
- **现象**：new-prompt tactile checkpoint 2000 的验证 loss 最低，但与 2999 做相同离线评估后，只在部分旋转长尾上更好，位置、夹爪和完整 chunk 指标更差。
- **根因**：flow-matching validation loss 是归一化训练目标，不等同于毫米、角度、夹爪越界率或任务成功率；不同 checkpoint 的误差分布也可能发生权衡。
- **解决方案**：checkpoint 选择同时看配对物理指标、P95/P99/max、安全越界率、分 episode 结果和最终真机成功率，不只看单一平均 loss。
- **验证**：2000 与 2999 使用相同 693 anchors、seed、noise、horizon、norm hash 和数据 hash 做成对比较。
- **面试讲法**：把优化目标、离线代理指标和最终业务指标分层，说明为什么三者可能不一致。

## 11. 触觉桥未启动导致 `stopped: "'left'"`

- **日期**：2026-09-08
- **状态**：已解决。
- **现象**：程序完成 server metadata 校验后立即退出，reason 为 `"'left'"`。
- **排查过程**：错误形态是访问 `frames['left']` 的 KeyError，而不是模型推理、shape 或 WebSocket 错误。用户确认当时触觉桥没有启动；随后完整 shadow 中左右触觉持续正常。
- **根因**：启动依赖缺失，程序尚未收到左触觉首帧。
- **解决方案**：按顺序启动触觉桥并检查 ROS topic；启动等待错误改为列出所有缺失流，而不是裸 KeyError。
- **验证**：后续 30 秒日志有 149 个有效触觉 chunk，左右统计均有限且非零。
- **面试讲法**：从异常类型和生命周期阶段判断问题层次，避免一看到“触觉”就怀疑模型或驱动数据格式。

## 12. checkpoint、norm stats 与 conversion 可能配错

- **日期**：2026-09-08
- **状态**：本次事故已排除；运行时校验已加入。
- **现象**：真机输出幅度异常，首先需要排除 checkpoint 使用了错误归一化统计、动作单位或转换配置。
- **风险原理**：模型学习的是归一化动作。错误的 q01/q99、mean/std 或错误的绝对/相对语义，会在反归一化后直接放大成真实空间跳变。
- **排查方法**：server 返回 config、checkpoint、action horizon、dataset fps、use_tactile、`norm_stats_sha256` 和 `conversion_sha256`；客户端连接时严格比对并写入日志。
- **结论**：当次 checkpoint 2000 的 norm hash 与本机两个对应 checkpoint 资产完全一致，conversion hash 也一致，因此“文件复制错了”被排除。它不能证明训练数据本身正确。
- **面试讲法**：用内容哈希和 provenance 把配置问题变成可证伪问题，不通过文件名或路径猜版本。

## 13. Shadow 中异步索引放大动作与当前状态距离

- **日期**：2026-09-08
- **状态**：机制已定位；需要在新模型和实际跟随条件下重新验证。
- **现象**：推理约 240～265 ms 后返回，控制器按时间选择 `action[2]` 或 `action[3]`；这些动作与当前 state 的距离明显大于 `action[0]`。
- **根因**：模型根据旧观测预测未来轨迹；正常执行时机器人应同时执行前序动作。Shadow 模式不移动机器人，因此未来动作与静止 state 的距离必然增长。推理延迟、限速导致的跟随落后和索引误差会进一步放大。
- **关键证据**：完整 30 秒日志中，action0/state 位置距离 p50 23.18 mm、p95 61.35 mm、max 142.86 mm；选中 action2/3 后为 p50 43.93 mm、p95 90.53 mm、max 377.38 mm。机器人 current 与 observation 的中位差只有 0.0021 mm。
- **解决方案**：第一阶段一次记录完整 50×7 chunk，同时记录 action0、实际 action[k]、当前 state、观测年龄和分解距离；2026-09-09 按部署需求改为同步推理，每份观测阻塞等待并固定使用 action0，完整 chunk 只保留作诊断。
- **验证范围**：异步问题已由代码路径移除并有 CPU 模拟测试；尚未用新实现运行现场 shadow，实际推理频率、延迟和闭环效果待验证。
- **面试讲法**：同步/异步问题本质是时间对齐。日志必须同时带观测时间、结果时间、执行索引和实测状态，单看最终 target-state 距离无法归因。

## 14. 相同观测下不同 action chunk 波动和切换跳变

- **日期**：2026-09-08
- **状态**：待解决。
- **现象**：机械臂几乎静止，相邻观测状态只差微米级，但不同推理返回的 action0 和选中目标变化较大；新旧 chunk 切换比 chunk 内相邻步更不连续。
- **证据**：相邻 observation 位置中位变化约 0.0022 mm，action0 跨 chunk 变化中位 19.97 mm、最大 131.84 mm；切换时选中目标变化中位 31.35 mm、最大 349.13 mm，而同 chunk 内中位 6.51 mm。
- **候选原因**：flow matching 采样噪声、受污染的 v1 标签、输入分布偏移、不同 chunk 缺少连续性约束。现有证据不能把它归因于某一个因素。
- **后续方案**：使用 v3+SO(3) 新模型；对同一冻结观测做固定噪声和多随机种子重复推理；比较 action0、完整 chunk 和跨 chunk 轨迹；再评估固定噪声、temporal ensemble、轨迹拼接或 replan 策略。
- **面试讲法**：用近似不变输入做重复实验，把传感器变化和模型采样方差分开。

## 15. 夹爪轻微越界掩盖位置和姿态错误

- **日期**：2026-09-08
- **状态**：已改为显式饱和限幅，待 shadow 和真机验证。
- **现象**：绝大多数控制 tick 首先报告单指位置超过 `[0,0.0425] m`，看起来像主要只有夹爪问题。
- **证据**：模型中位输出只比 42.5 mm 上限高约 0.13～0.17 mm，是小幅连续输出越界；但按独立条件重算后，同批日志还包含大量位置、姿态和 workspace 失败，只是被检查顺序遮住。
- **根因**：连续模型不会自动严格满足物理闭区间；日志只记录第一个异常会产生诊断偏差。
- **解决方案**：日志独立计算所有 guard 指标；有限的夹爪预测裁剪到单指`[0,42.5] mm`，位置、姿态和工作空间预测也投影到配置边界，然后继续做逐周期限速。日志同时保存原始预测、投影目标、最终命令和所有触发项。NaN/Inf、实测状态越界及跟踪故障继续停止。
- **验证范围**：边界投影和日志由 CPU 单元测试覆盖；尚未运行新版本 shadow 或发送真机命令，不能把“命令在边界内”等同于“模型输出正常”。
- **面试讲法**：安全检查顺序会影响“看到的故障分布”。诊断统计和执行短路逻辑应该分开设计。

## 16. 部署 prompt 与训练任务文本不一致

- **日期**：2026-09-08 定位；2026-09-09 提交修复。
- **状态**：已解决。
- **现象**：真机配置发送采集描述 `fr3 zed DM-Tac...`，而 new-prompt checkpoint 训练时使用具体装配指令。
- **根因**：部署配置沿用了旧数据采集 metadata，没有绑定训练集 `meta/tasks.jsonl` 的权威 task 文本。
- **解决方案**：部署默认 prompt 改为训练时的完整装配指令；v3 loader 从 task index 和 tasks mapping 读取 prompt；运行日志记录实际 prompt。
- **验证**：配置提交 `c7ff5cc`；JSON 和部署测试通过。
- **面试讲法**：多模态模型的文本也是输入 contract。模型结构和 checkpoint 对上了，prompt 不一致仍属于分布偏移。

## 17. 真机起始姿态超出训练分布

- **日期**：2026-09-08；2026-09-10 更新。
- **状态**：旧运行已定位；最新五次 K=2 已对齐起始姿态，因此本问题不是本轮失败原因。
- **现象**：真机 XYZ 接近采集起点，但模型输出仍高度不稳定。
- **证据**：旧运行的真机起始 rotvec 约 `[2.252,-2.136,-0.041]`，与全部6655个v1 states的最小SO(3)距离为38.65°。2026-09-09 v3-20000同步shadow的实测rotvec约`[2.236,-2.168,-0.031]`，与v3全部6626个states的最小物理旋转距离仍为39.76°（最近为episode 2 frame 248）。随后`sync_execute_004`起始状态约为`[0.3268,0.0854,0.4218,-2.3929,2.0311,-0.1196,0.0425]`；其姿态与v3最近状态仍差31.81°，XYZ与v3最近状态相距65.71 mm，z还比数据集全局最大值高约6.0 mm；与任一episode起始XYZ最近也相距84.59 mm。训练轨迹起始姿态自身最大分散仅约0.51°。2026-09-10 的 `sync_k2_execute_003`～`007` 与最近 v3 episode 起点的位置仅差0.17～1.47 mm、SO(3)姿态仅差0.09～0.53°，证明现场已经完成本轮起点对齐。
- **候选原因**：现场末端实际方向不同，或 TCP/base frame 定义与采集时不一致。
- **解决方案**：用遥操作恢复到采集起始姿态，核对 HTTP state 的 quaternion/rotvec 与 TCP 配置，然后用正确 prompt 再做 shadow；不能直接把模型 guard 放宽。
- **验证范围**：当前只证明机械臂 state 起点对齐；物体位姿、相机图像、光照、TCP 语义和触觉输入没有在 JSONL 中完整留档，仍需观测回放验证。
- **面试讲法**：位置接近不代表六自由度状态接近。分布检查要使用 SO(3) 距离，而不是只比较 XYZ 或 rotvec 分量。

## 18. 传感器时间偏差和旧 chunk 生命周期诊断不足

- **日期**：2026-09-08
- **状态**：传感器诊断已改善；同步推理移除了 active chunk 持续复用路径，待现场验证。
- **现象**：一次运行在 2.8 秒后因多模态 timestamp skew 超过 100 ms 退出，但旧错误没有指出具体流；另一次运行继续使用年龄达到约 391 ms 的已接受 chunk。
- **根因**：旧诊断只报告总体 skew；结果年龄校验主要发生在 inference future 完成时，已接受 chunk 在后续 tick 中可能继续变旧。
- **解决方案**：错误日志增加每路 sensor stamp、age、最老/最新流；每个 chunk 和 tick 记录 observation age。2026-09-09 改为每份观测同步产生一次 action0，不再跨 tick 复用 active chunk；每次结果返回立即检查350 ms年龄上限。
- **验证**：新日志能够定位每路时间信息；同步结果年龄门限有模拟测试，现场时序尚未验证。
- **面试讲法**：实时系统的有效性不仅由消息是否到达决定，还包括 freshness、skew 和结果生命周期。

## 19. v3 压缩缺帧与部署限速不匹配

- **日期**：2026-09-09
- **状态**：待解决或通过实验重新标定。
- **现象**：v3 的位置脏标签已经消失，但12条 episode 共存在18个缺失采样间隔，并被压缩到统一10 Hz时间轴；同时训练轨迹速度经常高于初始部署限速。
- **证据**：v3 最大一步位置变化45.501 mm，发生在被压缩的 episode 17；缺原始逐帧时间信息，尚不能证明该行正好跨越缺口。2726/6626，即41.14%的下一状态目标在100 ms内移动超过2 mm，而部署 limiter 只允许2 mm/100 ms。
- **影响**：模型预测的未来轨迹可能比执行端允许的速度快；异步控制中机器人会落后于模型假设的未来 state，重新产生 target/current 距离增长。
- **解决方向**：后续严格导出应在时间缺口处分段或保留真实时间；先保持安全 limiter 做 shadow 和跟踪误差测量，再根据机器人底层安全约束审查控制频率、插值和速度，而不是裁剪训练标签来迎合临时 limiter。
- **面试讲法**：数据时间轴和控制器时间轴必须一致。仅修复数值标签，若动态特性不匹配，闭环仍会失败。

## 20. Tokenizer 测试默认缓存目录只读

- **日期**：2026-09-09
- **状态**：已解决。
- **现象**：SO(3)、v3 配置和部署组合测试中有2项 tokenizer 测试失败，堆栈停在创建 `/home/yanghaojun/.cache/openpi`，报 `OSError: Read-only file system`；其余52项通过。
- **根因**：测试进程没有继承项目约定的缓存位置，OpenPI 回退到用户默认缓存目录；当前受限执行环境只允许读取该目录。已有 tokenizer 实际位于 `/data/yanghaojun/cache/openpi`。
- **解决方案**：测试命令显式设置 `OPENPI_DATA_HOME=/data/yanghaojun/cache/openpi`，保持模型文件和项目缓存路径一致。
- **验证**：设置正确环境变量后，同一组SO(3)、v3配置、真机部署和服务测试54项全部通过。
- **面试讲法**：先读最深层异常和失败发生位置。测试名与 tokenizer 有关不代表 tokenization 算法错误；失败发生在 `mkdir`，应先检查缓存路径和权限。

## 21. v3 4000/20000 checkpoint 指标冲突

- **日期**：2026-09-10（运行发生于2026-09-09晚间）。
- **状态**：离线物理指标已完成配对选择；多随机种子、shadow 和真机验证尚未进行。
- **现象**：4000 步的归一化 flow-matching 验证 loss 为全程最低的 0.29023，20000 步升至 0.44452；但相同离线动作评估中，20000 步的首动作和完整 chunk 三项平均物理误差均低于 4000 步。
- **证据**：两次评估的 690 个 anchors、episode/frame、target、state、valid mask、数据与 norm hash、seed 42、10 次去噪和 50 步 horizon 完全一致。20000 相对 4000 的首动作位置/旋转/夹爪均值降低 18.20%/4.22%/20.01%，完整 chunk 降低 7.07%/2.79%/10.79%；首动作位置配对胜率 72.46%。但 episode 24 的 chunk 位置均值从 45.68 mm 升至 49.73 mm，20000 还出现 7.86° 的深 horizon 单点旋转异常。
- **根因判断**：验证 loss 与反归一化后的采样动作误差不是同一目标；固定噪声的一次策略采样也不能代表完整随机输出分布。验证 loss 持续上升能证明模型在该训练代理目标上变差，但现有证据不能把物理指标冲突唯一归因于过拟合或采样运气。
- **解决方案**：当前两者中以 20000 作为下一轮受保护 shadow 候选，保留 4000 作为回退；补评 8000/12000/16000，并对候选 checkpoint 使用多个推理 seed 重复相同离线评估。checkpoint 排序同时看首动作、完整 chunk、分 episode、P95/max、越界率和最终 shadow/任务成功率。
- **验证范围**：这里只完成保存预测的 CPU 数值配对、评估日志/图检查和训练日志分析。没有重新运行模型、没有 shadow、没有发送机器人命令，因此不能声称 20000 的真机成功率更高。
- **面试讲法**：模型选择不能只看训练代理 loss。先固定样本和采样设置做逐样本物理量比较，再检查长尾和分场景退化，最后把候选带入受保护 shadow 与真实任务指标验证。

## 22. 预测饱和、同步推理与 chunk 步数协调

- **日期**：2026-09-09。
- **状态**：同步K=1代码、CPU回归、30秒shadow和首次20秒真机执行已完成；闭环任务质量与K=2待验证。
- **现象**：旧客户端对有限但越过夹爪、workspace、目标位移或目标旋转阈值的预测直接拒绝；异步循环在推理期间继续使用旧 chunk，并按观测年龄选择 action2/action3。用户希望边界外预测输出边界值并改成同步控制，还需要确定每个50步 chunk实际执行前几步。
- **证据**：旧`TargetGuard.prepare()`在限速前对夹爪、workspace、5 cm目标位移和0.35 rad目标旋转抛出`ValueError`；旧控制循环用后台 future，并通过`floor(observation_age*10)`选择未来索引。既有真机 shadow 的推理年龄约239～265 ms，因此主要选择action2/action3。v3 checkpoint 20000离线结果中，action0/action1相对anchor state的最大位置距离为22.86/35.04 mm且均无样本超过50 mm；action2开始出现1个超过50 mm，action3有10个。预测对目标的位置误差均值在action0～3依次为4.62/8.21/11.52/14.44 mm。
- **根因**：旧实现采用“预测越界即停”的安全策略，并为降低推理延迟采用异步短 chunk；它与当前希望的“有限预测饱和 + 每次当前观测同步action0”控制语义不同。
- **解决方案**：有限预测依次投影到XYZ workspace、相对实测位置5 cm、SO(3)最短旋转0.35 rad和单指`[0,42.5] mm`，再按每周期位置、旋转和夹爪速度限幅；日志保留raw、bounded、limited三层动作和全部`limits_applied`。控制循环改为一份观测对应一次阻塞推理，初始固定执行action0，即K=1。推理期间没有执行新chunk前缀，因此不能按结果年龄跳到action1/action2。同步shadow证明跨chunk波动、跟踪和饱和比例可接受后，才试K=2：action0后等待100 ms，重新读取状态并检查tracking/enable，再执行action1并重新采样推理。实测workspace/夹爪非法、跟踪误差、NaN/Inf、传感器/推理超时、心跳丢失和HTTP失败继续停止。
- **验证范围**：CPU模拟覆盖所有预测边界、同步索引0、慢推理拒绝、心跳中断、HTTP部分失败和shadow零写入；保存的v3预测完成action0～7逐horizon分析。现场K=1 shadow完成150次推理；首次execute附件含93个完整成功控制周期，限速和跟踪门限按预期生效。K=2、接触过程、完整任务成功率和底层控制器内部安全状态仍未验证。
- **面试讲法**：动作chunk索引由机器人真实执行进度决定，不能把推理耗时直接换算成跳过的动作步。先用同步action0形成低频闭环，再以跟踪和饱和日志决定是否增加到K=2；需要连续10 Hz时应采用流水线或底层时间戳轨迹接口。

## 23. v1 服务配置加载 v3 checkpoint 20000

- **日期**：2026-09-09。
- **状态**：已解决，并经推理主机真实checkpoint加载及30秒shadow验证。
- **现象**：服务以`--config pi0_lora_tacfield_local_tactile_lora_smoke`和 checkpoint 20000 启动，在模型参数恢复前报缺少`actions/state/tactile_prefix`归一化统计。
- **证据**：日志明确尝试读取`assets/local/tabero_lerobot_compact_v1`；实际20000来自`pi0_lora_tabero_v3_touch_20k/v3_touch_so3_20k_run1`，其统计文件位于`assets/local/tabero_lerobot_compact_v3/norm_stats.json`，SHA256为`3179b7ee7553cac64f3ac8a40897ed620b78b2ee354f9a297534f88cc66a0aed`并包含三组所需统计。
- **根因**：checkpoint路径换成20000后仍沿用旧v1 config。config决定`asset_id`、SO(3)动作正反变换、触觉LoRA结构和模型恢复契约，因此不能只替换checkpoint目录。
- **解决方案**：服务改用`--config pi0_lora_tabero_v3_touch_20k`；将该v3配置、SO(3)变换和测试提交到部署分支。资产错误现在同时报告配置期望目录、实际可用目录和配置名，服务metadata新增`asset_id`。
- **验证范围**：本地读取确认20000资产结构和统计键；CPU测试覆盖移动后的v3 checkpoint资产加载及v1/v3错配提示。推理主机随后成功报告v3 config、对应norm hash和50步输出，并完成150次同步推理，说明配置、统计和参数已真实加载；任务成功率仍未验证。
- **面试讲法**：checkpoint不仅是权重。部署必须把权重、模型配置、动作变换和归一化统计作为一个不可拆分的版本化资产，通过`asset_id`和hash在启动阶段阻止混配。

## 24. v3-20000 同步 shadow 的时延、限幅与随机波动

- **日期**：2026-09-09。
- **状态**：配置和基础安全门限通过；起始姿态分布偏移与相邻预测波动待处理，尚不建议直接执行。
- **现象**：30秒同步shadow完成150次推理，控制台反复显示约192～207 ms延迟，部分周期出现`limits=['max_translation_m_s']`；用户需要判断这是否是报错及能否执行。
- **证据**：控制台无traceback，`execute=false`；所附JSONL因粘贴截断包含143个完整tick和第144个残缺chunk。在143个完整tick中，全部推理结果被接受且`ok=true`，HTTP pose/gripper发送均为0。推理延迟均值197.15 ms，结果年龄最大306.57 ms，低于350 ms门限。action0相对当前状态的位置误差中位1.38 mm、P95 4.67 mm、最大8.93 mm；SO(3)角度中位0.054°、最大0.421°；单指差中位0.552 mm、最大1.187 mm。37/143个tick触发2 mm/周期平移限幅，1个触发夹爪速率限幅；没有workspace、5 cm目标距离、0.35 rad旋转或夹爪物理范围裁剪。相邻action0位置变化P95 5.32 mm、最大9.12 mm，142对相邻平移方向中50对点积为负，说明近静止输入下仍有明显采样波动。实测姿态与v3数据集最近状态仍相差39.76°。
- **根因或假设**：约5 Hz是同步K=1在约197 ms单次推理下的预期吞吐，不是控制循环故障。相邻预测波动可能来自flow-matching随机采样和姿态/场景分布偏移；现有一次shadow不能证明各自贡献。
- **解决方案**：保留同步K=1和2 mm逐周期限幅；先把机器人恢复到训练姿态并重复shadow，再对冻结观测做多次推理量化纯采样方差。后续日志记录服务端`asset_id`。只有对齐后饱和比例和相邻action0波动可接受，才在硬件停止和持续使能下做极短执行。
- **验证范围**：这是shadow，机械臂未执行预测；不能从静止输入日志推断闭环跟踪、碰撞安全或任务成功率。统计基于附件中143个完整tick，控制台显示完整运行达到150次。
- **面试讲法**：先用`execute`标志、发送计数和异常栈判断是否真正故障，再把实时性能拆成推理延迟、结果时效、预测幅度、限幅率和跨次方差；基础门限通过仍需检查训练分布和闭环行为。

## 25. 首次同步 execute 接近 workspace 下界

- **日期**：2026-09-09。
- **状态**：20秒执行未触发客户端故障并进入退出阶段；完成日志已补强。机械臂已接近x工作空间下界，暂停重复执行并待人工复位、完整终端尾行确认和对齐姿态后复测。
- **现象**：用户在未先消除约40°起始姿态分布偏移的情况下运行`--execute --seconds 20`。终端完成约94次同步推理后截断在最后一条`WARNING`，JSONL附件也在chunk94中截断。
- **证据**：附件包含93个完整control tick，全部`ok=true`并发送pose，69个发送gripper；没有`stopped`异常记录。实测末端在附件完整区间内累计移动`[-7.58,-1.23,-22.82] mm`，物理旋转2.49°，夹爪总宽收紧6.37 mm。逐次command位置变化最大2.000 mm、姿态最大0.573°、夹爪总宽最大2.000 mm，符合配置上限；上一命令相对当前实测的跟踪误差最大2.394 mm和0.446°，远低于30 mm和11.46°停止阈值。39/93次触发平移速率限幅、3次夹爪速率、1次旋转速率；没有workspace、5 cm、0.35 rad或夹爪物理范围投影。推理均值209.1 ms，结果年龄最大312.8 ms。最后完整实测x为0.300631 m，采样中最低0.300557 m，距配置下界0.300 m仅约0.56 mm。
- **根因或假设**：执行轨迹主要沿负z并同时靠近x下界；客户端限速和跟踪保护按设计工作。相邻action0位置变化P95约6.95 mm、最大13.94 mm，92对相邻目标方向有42对点积为负，支持预测抖动仍存在。最后截断的`WARNING`按代码和20秒时间对应最可能是best-effort measured-pose hold，但附件不完整，不能声称已直接看到完整文本。
- **解决方案**：不从当前位置重复执行；先用人工控制回到采集起始六自由度姿态并留足workspace余量，核对终端完整尾行和机器人状态。保持K=1、2 mm/周期限幅和持续使能；在下一次执行前降低夹爪命令抖动并用对齐姿态shadow复核相邻预测。客户端已增加`asset_id`和`completed`事件，明确记录完成原因、控制周期、pose/gripper发送次数及最终hold结果，避免依赖截断终端判断退出状态。
- **验证范围**：只分析用户提供的93个完整tick和终端摘要；第94个chunk、最终hold响应、现场物体接触和硬件控制器内部状态未完整提供。未从本机发送任何机器人命令。
- **面试讲法**：真机首跑先检查命令是否满足逐步上限，再检查跟踪误差、累计位移、工作空间余量和退出路径。单步都安全并不保证累计轨迹安全，靠近workspace边界时应停止复位，而不是继续依赖裁剪顶住边界。

## 26. 长时同步 execute 因 ZED 前视帧延迟触发传感器 skew 停止

- **日期**：2026-09-09。
- **状态**：客户端按设计停止并发送保持位姿命令；ZED 前视帧短时延迟的上游根因尚未定位，230秒长时执行未完成。
- **现象**：`sync_execute_004`请求执行230秒，但约8秒、控制台报告40次同步推理后退出。异常为`Sensor capture timestamps are not synchronized: skew=0.117s`，随后出现Python traceback。
- **证据**：停止瞬间前视`front`帧年龄144.877 ms，腕部图像30.834 ms、机器人状态27.783 ms、左右触觉57.373/58.736 ms；因此最老的前视帧和最新状态相差117.094 ms，超过`max_sensor_skew_sec=100 ms`，但各流自身仍低于250 ms年龄上限。控制台明确记录`Sent best-effort measured-pose hold`。所附JSONL因粘贴截断只有39个完整tick及第40个残缺chunk；39个完整tick全部`ok=true`并发送pose，28个发送gripper。推理延迟均值198.27 ms、结果年龄最大304.10 ms；35/39次触发平移速率限幅、3次触发旋转速率限幅，没有workspace越界。上一命令到当前实测的跟踪误差最大4.06 mm和0.60°，远低于30 mm和11.46°停止阈值。机械臂在完整记录内累计移动约`[+14.26,+3.09,-11.51] mm`、旋转1.29°、夹爪总宽收紧1.87 mm。
- **根因或假设**：直接触发条件已确定为前视帧相对其他输入延迟，不是模型目标越界、机器人跟踪误差或启动时DDS type-hash警告。前视相机发布抖动、ROS/DDS传输短时丢帧或重传、以及客户端图像解码/线程调度延迟都是候选原因；当前没有同一时段的topic频率和delay序列，不能确定上游根因。起始状态仍存在明显训练分布偏移，但它与本次传感器skew停止是两个独立问题。
- **解决方案**：当前fail-closed行为保留：不使用时间错位观测继续推理，退出时发送一次实测位姿hold。部署主机需先拉取提交`78c8d5c`，以后异常会在JSONL明确写`stopped`事件；本次metadata缺少`asset_id`且行号仍对应旧代码，说明该主机尚未更新。复测前分别对ZED前视topic运行`ros2 topic hz`、`ros2 topic delay`和`ros2 topic info --verbose`，确认发布频率、延迟尾部和QoS；先不要直接把100 ms阈值调大。若确认只是单帧偶发延迟，再设计“保持上一目标并等待下一组有效同步样本”的有界恢复机制，并在shadow验证后用于真机。
- **验证范围**：已从终端确认停止原因和hold请求，从附件39个完整tick确认停止前限速、跟踪与HTTP发送正常；没有硬件急停状态、完整原始JSONL尾部、ZED同时间段频率/延迟统计或现场任务结果，因此不能声称相机根因已修复或任务成功。
- **面试讲法**：多模态真机控制既要检查每路数据是否新鲜，也要检查它们是否描述同一时刻。这里所有传感器都没有单独过期，但前视图像比机器人状态慢117 ms；客户端拒绝这组错位观测并保持当前位姿，从而把感知时序故障隔离在动作发送之前。随后用每个topic的频率、延迟和QoS定位上游，而不是先放宽安全阈值。

## 27. 同步 K=1 控制吞吐受推理延迟限制

- **日期**：2026-09-10。
- **状态**：K=2实现和控制稳定性已验证；随后五次完整/长时任务均失败，K=5仍不满足当前观测时效门限。
- **现象**：同步部署每次约200 ms推理后只执行一个10 Hz标签步，用户观察到动作明显偏慢，希望增加每次推理执行步数。
- **证据**：v3标签的`actions[t]`是下一帧绝对状态，数据集频率10 Hz，因此chunk中相邻动作的语义间隔为100 ms。`sync_execute_004`推理均值198.27 ms，39个完整tick中35个触发每周期2 mm平移限速；K=1约每200 ms只推进一个标签步，动作吞吐约5 Hz，且`TargetGuard`把延迟周期的`dt`封顶为100 ms，防止通过卡顿产生追赶大步。随后`sync_k2_execute_001`完整运行120秒并正常结束，404次推理产生750个控制步和750次pose命令，实际6.25 Hz；在控制台第400次推理时累计744步，说明344/400（86%）的推理块执行了第二步。用户粘贴的JSONL只覆盖前93个完整推理块和160个tick，其中67个执行`action[1]`、26个记录`chunk_truncated`：action0观测年龄均值235.00 ms，成功action1均值319.34 ms/最大348.01 ms，截断action1均值374.66 ms。该片段160个tick全部`ok=true`，119次平移限速、4次旋转限速；逐条命令最大2.000 mm和0.573°，上一命令跟踪误差最大4.44 mm和0.573°，没有原始目标workspace越界。按相同时序，K=5的action2平均约435 ms、action4平均约635 ms，都会越过350 ms门限。
- **根因或假设**：当前速度主要受阻塞式推理、每chunk消费的动作数和20 mm/s客户端平移上限共同限制。高限速率说明速度上限也在起作用，因此K=2将吞吐从约5 Hz提高到6.25 Hz，但仍不能越过配置速度上限。K=5若保持350 ms时效检查，实际仍会在前两步后截断；若为了执行五步而放宽到约650 ms，则视觉/触觉高层闭环降到约1.7 Hz。任务视觉效果是否还受模型目标幅度或底层控制器速度限制，当前日志不能确定。
- **解决方案**：新增`--actions-per-inference {1,2}`，配置默认1。K=2按`action[0]`、等待100 ms、`action[1]`执行；每一步重新读取实测状态并运行完整TargetGuard和持续使能检查。后续动作若超过`max_result_age_sec`则记录`chunk_truncated`、丢弃剩余旧动作并立即重规划。禁止K>2，也禁止缩短数据集100 ms动作间隔或一次性连发。日志增加请求索引、实际`chunk_index`、prediction距离、inference/control计数和截断原因。
- **验证范围**：CPU测试确认K=2索引顺序为0/1、间隔100 ms、每步重新执行控制路径，以及350 ms后第二步被丢弃并重规划。后续五个完整 JSONL 共记录5,875个成功 pose 控制步，实际频率5.97～6.18 Hz，第二步执行率80.5%；客户端跟踪稳定但五次任务均失败。该结果验证了控制链路，未证明 K=1 会成功或 K=2 单独导致失败；K=3～5没有shadow或真机验证。
- **面试讲法**：阻塞推理200 ms而标签周期100 ms时，K=1只能提供约5 Hz动作。直接批量发送多步会破坏时序和安全检查，因此选择K=2折中：按训练频率逐步发送、每步读取反馈并检查安全，过期就截断重规划；K值由推理延迟、标签频率、观测时效和接触任务的反馈需求共同决定。

## 28. 离线动作误差小但五次 K=2 真机任务失败

- **日期**：2026-09-10。
- **状态**：已定位离线指标解释差异、控制链路状态和闭环漂移；真机任务失败的最终根因仍需通过观测回放、阶段指标和成对时序试验定位。
- **现象**：checkpoint 20000 的离线评估看起来只有约4.62 mm第一步位置误差，但`sync_k2_execute_003`～`007`五次真机均没有完成抓取/装配，机械臂也没有到达目标终点。用户怀疑推理频率和每次执行步数。
- **证据**：五次使用完全一致的config、checkpoint、v3 asset、prompt、norm SHA和conversion SHA，且起点与最近示范起点只差0.17～1.47 mm和0.09～0.53°。机械臂实际净移动310～353 mm、累计路径511～727 mm，5,875条pose命令全部成功；位置跟踪p95为5.0～5.8 mm、最大8.3 mm，说明不是“没有发命令”或底层不跟随。末态与最近专家终点仍差104～188 mm和10～20°；沿途最近训练state的位置差p95达到25～53 mm，表明闭环逐渐离开示范分布。控制实际只有5.97～6.18 Hz，K=2第二步执行率80.5%，634个chunk被截断；62.2%的控制步触发限速。运行005的单指位置始终在40.63～42.50 mm，未达到示范中27.52～34.64 mm的闭合范围。运行007另因ZED/状态skew 102 ms停止。
- **根因或假设**：已经确认“离线看起来小”的直接原因是指标口径：checkpoint 20000离线结果只在专家记录观测上评估690个anchor，第一步平均误差为4.623 mm/0.163°/0.442 mm，而保持当前位置不动的第一步基线反而更小，为3.627 mm/0.126°/0.081 mm；完整有效chunk平均误差为38.622 mm，位置p95为101.286 mm。该测试不让模型承受自身动作造成的新观测，也不测是否抓取或插入。真机任务失败中已经观察到闭环漂移和夹爪阶段错误；约6 Hz与训练10 Hz不匹配、高限幅率及K=2旧观测复用是有证据的贡献因素，但没有同场景K=1/K=2对照，不能认定它们是唯一根因。相机画面、物体位姿、光照、触觉语义、TCP/标定偏差仍是明确标注的待验证假设，因为现有JSONL没有保存传感器payload。
- **解决方案**：停止重复长时execute，先重写推理客户端的可诊断链路：保存同步ZED/D405/完整触觉/state观测包及时间戳，记录分阶段端到端延迟、推理seed、raw/bounded/limited动作、实际action索引和夹爪实测值；建立同一观测的旧/新客户端回放。按reach、pre-grasp、close、transport、align、insert报告阶段结果；先K=1 shadow，再K=2 shadow，只有结果年龄、动作连续性和阶段方向通过后才做短时受保护execute。正式K=1/K=2比较必须固定起点、物体、相机、光照、checkpoint和安全限制。
- **验证范围**：日志完整性、模型资产一致性、起点对齐、机器人移动/跟踪、K=2利用率、限幅率、末态偏差、路径偏移、运行005夹爪行为及匹配的离线summary已做只读验证。尚未保存/检查五次现场RGB和完整触觉，未做冻结观测重复推理、K=1/K=2成对任务试验、分阶段成功率或重写客户端现场shadow。
- **面试讲法**：先把“机械臂没到目标”拆成模型输出、客户端限幅、命令接受、底层跟踪和任务语义。日志证明机械臂执行了大量命令且跟踪正常，但策略闭环离开示范并停在错误终点；随后发现4.62 mm只是专家观测上的短时第一步平均误差，甚至不如保持不动基线。解决方向不是盲目增加K，而是补齐观测回放和阶段成功指标，再用受控成对试验区分模型泛化、时序和限幅贡献。

## 29. v3 checkpoint 与推理主机旧代码的 SO(3) 契约漂移

- **日期**：2026-09-10。
- **状态**：A6000 当前分支已排除代码缺失；用户报告的推理主机 checkout/启动目录尚未直接核对。现有客户端 provenance 拒绝条件不完整，修复前禁止真机 execute。
- **现象**：推理主机代码分析显示没有 `pi0_lora_tabero_v3_touch_20k` 配置，姿态恢复仍是 rotvec 三分量相加；checkpoint 和 `norm_stats.json` 哈希却与 v3 交接文档一致。需要判断正确资产是否足以保证部署兼容。
- **证据**：A6000 当前 HEAD 为 `53370e6`，包含引入 v3 配置和 SO(3) 变换的提交 `badb9ef`；当前配置实际选择输入 `RelativePoseActions` 和输出 `AbsolutePoseActions`，后者执行 `R_action = R_state * R_relative`。`badb9ef` 的父提交只含 `DeltaActions/AbsoluteActions`，没有 v3 配置或 SO(3) 类，与报告症状吻合。22项 CPU 配置、变换和服务测试通过。对全部6626个 v3 一步专家标签回放旧逆变换，SO(3)误差中位0.056°、P95 0.410°、最大1.445°；在有效 horizon-49 专家目标上P95 4.348°、最大6.672°。这证明旧相加不是等价实现，但不能据此推断模型任意预测的误差上界。
- **根因或明确假设**：权重和归一化文件不保存 Python 配置或动作逆变换，资产哈希相同只能证明数值资产一致。若推理主机事实描述准确，直接原因是运行了 `badb9ef` 之前的代码、另一个工程目录或未同步分支；在获得该主机 `pwd`、HEAD 和进程命令前，这仍是有强证据的版本漂移假设，不是已远程确认的根因。正常旧 v1 配置配未改目录的 v3 checkpoint 会因 asset id 不同而启动失败；若人工复制/改路径让旧配置读到 v3 stats，则可能无异常运行却静默输出错误姿态。客户端目前没有强制比较 `config`、`asset_id`、norm hash 和 `training_action_representation`，只记录其中部分字段，因此不能完全 fail-closed。
- **解决方案**：推理主机必须使用包含 `badb9ef` 的受控提交（建议与当前已审查 HEAD 对齐），以 `--config pi0_lora_tabero_v3_touch_20k` 启动，并在服务启动日志同时确认 v3 asset id、norm/conversion hash 和 SO(3)训练表示。后续代码应把这些字段及源码/transform fingerprint 加入客户端硬拒绝条件，而不是只写日志；此增强尚未实现。完成配置探针、SO(3)正反往返和服务 handshake CPU 测试后只能先做 shadow，不能直接 execute。
- **验证范围**：已完成当前 A6000 源码、提交历史、配置管线、客户端校验代码的静态检查，运行22项CPU测试，并用v3标签做几何回放；没有登录或读取用户所说的推理主机，没有加载模型、运行推理/服务、shadow或真机命令，因此不能断言已发生的五次执行使用了错误逆变换，也不能声称推理主机已经修复。
- **面试讲法**：模型部署资产不只有权重和归一化统计，动作坐标变换也是模型接口的一部分。同一组参数在错误的 SO(3) 逆变换下可以正常出数却产生系统姿态误差，所以需要把 checkpoint、config、transform 源码和统计哈希作为一个版本化单元，在启动握手阶段 fail-closed。

## 30. 白板 next-state 与 sent-command 配对训练

- **日期**：2026-09-15；2026-09-16复核新的一卡r32/12k配置。
- **状态**：新的一卡r32/12k `next_state`首次完成12000次更新与最终验证，但12000 checkpoint因临时目录竞态未提交；修复后已从完整8000点恢复并重新进入训练，`sent_command`尚未启动，等待新的12000原子提交实测。
- **现象**：同一批擦白板采集被导出为两套12126帧/39轨迹数据：`next-state`用机械臂下一帧实际到达状态监督，`sent-command`用同步记录的遥操作绝对目标监督。需要从相同Tabero 49999权重、相同超参和相同验证轨迹训练两个独立模型，并在第一套成功结束后无人值守启动第二套。
- **证据**：两套数据的episode长度、state、timestamp、frame/episode/global/task索引逐行一致；next-state的全部非末行action与下一state精确相等。seed 42一次性随机抽出验证episode `(1,7,17)`，两套均为1001验证帧和11125训练帧。标签分布差异很大：next-state目标相对state的位置p50/p95/max为5.21/14.50/46.74 mm，sent-command为40.78/380.97/473.38 mm；sent-command的39/39条轨迹共有161个相邻指令跳变超过50 mm，最大451.68 mm。episode 1在实测z约0.142 m时记录连续0.600 m命令，随后跳回约0.148 m。
- **根因或明确假设**：两套动作差异本身是实验变量；但sent-command中的0.6 m平台和数百毫米跳变不像普通一步跟踪延迟。指令限幅、VR使能门控、reset或未执行目标是待原始控制日志验证的假设，当前不能确定唯一根因，也不能把未来性能差完全归因于“命令监督优于/劣于状态监督”。
- **解决方案**：原实验新增两个独立asset/config/checkpoint命名空间，固定同一验证episode，分别计算训练集13D目标统计，并统一采用SO(3)相对旋转、6D wrist-wrench监督、触觉LoRA、全局batch 4、20k和4k保存策略。新的受控实验只把触觉TCN LoRA从rank/alpha 16提高到32，一卡全局batch 2，训练12k并每4k保存；新启动器使用`target_steps`同时控制训练、衰减和最终checkpoint判断。配置工厂已删除重复的旧`eval_num_batches=250`，保留`1_000 // batch_size`；新配置每次保存后等待原子提交，再继续训练或启动第二个模型。按用户要求原始sent-command标签不裁剪、不修复。
- **验证范围**：新配置已真实完成normalization、49999加载、rank32单卡初始化、W&B上传及next-state 12000次更新；4000与8000 checkpoint完整提交，12000仅为临时目录。修正后的配置、启动顺序、contracted-signal callback与保存等待合约共20项CPU测试通过。尚未从8000恢复、提交新的12000、启动sent-command、离线推理、shadow或真机执行，因此仍无完整A/B或任务成功率结论。
- **面试讲法**：对动作定义做A/B实验时，必须固定输入、episode split、初始化和超参，同时让归一化与checkpoint隔离。还要先审计标签分布；如果sent-command流含控制门控或饱和伪影，模型差异反映的是整个记录管线，而不是单一动作定义。

## 31. 白板训练遗漏 `wrist_wrench` 力/力矩监督

- **日期**：2026-09-15。
- **状态**：13D训练数据流、损失配置、输出拆分和CPU合约验证已完成；两次20k训练、力预测离线指标、坐标语义确认、shadow和真机验证尚未进行。
- **现象**：最初为白板A/B实验建立的配置继承v3 action-only目标，明确设置`supervised_action_dim=7`、`tactile_loss_weight=0`并从加载列中排除`wrist_wrench`。即使数据文件保留了6D wrench，该版本也不会学习力/力矩预测。
- **证据**：两套数据都声明`wrist_wrench: float32[6]`，顺序为`Fx,Fy,Fz,Tx,Ty,Tz`，来源为同步的`robot.force + robot.torque`。全量12126行均为有限值，两套导出逐行完全一致；三个力分量全局范围约`[-11.335,12.954]`，三个力矩分量约`[-2.822,3.834]`。原配置只查询`actions`序列并把第7维后的模型输出全部当无监督padding，因此“字段存在”不等于“已用于训练”。
- **根因或明确假设**：直接原因是第一版需求明确只预测7D动作，配置和loader按此实现；用户后续才补充开启力预测。数据元数据本身没有写单位和参考系，用户于2026-09-15补充确认为力用N、力矩用N·m、表达在K坐标系，配置把它记录为外部权威契约。重力和零偏补偿状态仍未确认，不能从数值范围推断。
- **解决方案**：新增专用输入变换，把同一查询时间序列的`actions[7]`和`wrist_wrench[6]`拼成13D监督目标；只对前6个姿态量应用XYZ/SO(3)相对变换，夹爪与wrench保持原语义。数据加载同时查询两条序列，只用36条训练episode计算13D action统计。模型恢复Tabero原有7D动作+6D力拆分损失，wrench权重0.1、padding权重0；推理输出重新拆成`actions[7]`和`wrist_wrench[6]`，避免机器人桥误把13维整体当动作。配置名、asset和checkpoint路径增加`force`，防止复用旧7D统计或运行目录。
- **验证范围**：CPU单元测试覆盖13D拼接、缺失/非有限wrench拒绝、SO(3)正反变换不改变后6维、7D/6D输出拆分、配置损失与顺序启动器；真实episode 0 loader确认得到`actions[50,7]`和`wrist_wrench[50,6]`并正确拼成`[50,13]`。尚未运行全数据视频审计和最终normalization，也没有构建GPU模型、加载参数、训练、上传W&B、推理或发送机器人命令。
- **面试讲法**：多任务输出不能靠数据里“有一列”自动生效；要同时检查query horizon、目标拼接、归一化、损失掩码和推理解码。这里把7D动作与K系6D wrench显式版本化，并在输出端重新拆开；元数据缺失的单位/坐标信息则通过带来源的外部契约补齐，而不是靠数值猜测。

## 32. 白板 checkpoint 异步提交失败与 W&B crashed

- **日期**：2026-09-16。
- **状态**：旧两卡运行已停止且最后完整点为12000；新的一卡r32已从8000恢复、重新完成next-state并原子提交12000，随后自动启动sent-command且更新finite。checkpoint目录竞态修复已通过真实保存验证；同一W&B run低步号续训导致历史被忽略的问题仍待改进。
- **现象**：W&B run `h5gj1m6i`显示`crashed`，顺序启动器没有继续第二个模型。本地`status.txt`却写成`state=complete, stage=train_next_state, exit_code=0`，与产物不一致。
- **证据**：旧运行日志在01:30:00完成step 16000验证（`action_loss=0.293907`、总`loss=0.072256`），启动Orbax异步保存后继续记录到显示step 16070/metrics step 16071；所有finite标记均为1。旧运行的4000、8000、12000完整，16000只有临时目录；VS Code remote agent与W&B时间线支持会话清理最终结束该运行。新r32运行的日志与tmux pane都以step 12000保存的`FileExistsError` traceback结束；4000/8000完整而12000只有临时目录，且没有第二个run/log/checkpoint。宿主复核发现新进程PID 556792仍处于`Sl+`/`futex_wait_queue`，GPU 2约占34 GiB但利用率0%，父bash在`do_wait`，说明它卡在异常后的线程退出而非继续训练，也解释了`status.txt`为何仍停在`running/train_next_state`。数据盘空间和inode充足，排除简单磁盘满。
- **根因或明确假设**：旧运行由save/finalize停滞和无tmux的VS Code会话清理串联造成，原始停滞的唯一内部根因仍未证明。新r32运行则有完整 traceback：先前修订让assets callback立即写入尚由Orbax异步创建的item临时目录；4000和8000中Orbax碰巧先创建成功，12000中callback抢先创建父目录，后台`atomicity._create_paths`再调用`os.makedirs(..., exist_ok=False)`，明确抛出`FileExistsError`。这次不是网络、OOM、NaN或GPU故障，而是已复现的目录创建竞态。
- **解决方案**：旧两卡checkpoint的一卡恢复仍需目标sharding修复。新r32 callback把`jax.process_index()`留在当前线程，避免worker-thread JAX初始化风险；实际文件callback重新包装为`CommitFutureAwaitingContractedSignals`，等待Orbax完成item临时目录创建后才写normalization，从而同时消除JAX线程死锁与目录竞态。两个新配置继续在每次4k保存后调用`wait_until_finished()`，且启动器只有在正式`12000/params`存在时才进入sent-command。当前先在原tmux中中断残留PID，再在一张空闲卡上用原pair名和`--resume`从完整8000继续；不改三卡拓扑，不手工把临时12000改名，也不让新旧进程同时访问同一run目录。
- **验证范围**：修正后contracted-signal延迟、非主进程跳过、保存等待、配置与启动器合计20项CPU测试通过；续训在GPU 0恢复8000后完成全部finite更新，新的12000在43.60秒内原子提交并含正式metadata/manifest，随后sent-command成功开始。W&B run `2u1yst8r`确认resume成功，但远端当前step已为12001，故拒绝重算区间8000–12000的低步号history；本地日志与checkpoint不受影响。sent-command尚未完成，离线推理、shadow和真机仍未验证。
- **面试讲法**：先区分观测系统状态和训练事实。W&B的crashed只是父进程异常消失的结果；真正决定恢复点的是checkpoint原子提交标记。这里通过日志时间线、finite遥测和Orbax目录结构证明模型没有数值崩溃，而是卡在16000异步保存，最终从12000恢复，同时把状态文件和checkpoint完成性绑定，避免“状态写完成但资产不可恢复”。

## 33. 单条 LeRobot 样本直接打印导致大数组淹没终端

- **日期**：2026-09-16。
- **状态**：已解决查看方法；未修改数据集。
- **现象**：需要查看 `tabero_lerobot_compact_v3` 和 `test2_tabero_next_state_compact` 的一条样本，但直接打印整条记录会展开触觉 marker 与深度数组，单帧即可产生数十万字符，数值与图像也分别存放在 Parquet 和 MP4 中。
- **证据**：两个数据集均为 LeRobot v2.1 目录；第0条实际包含 `state[7]`、`actions[7]`、`tactile_marker_motion[9,198,2]`、`wrist_wrench[6]`、`tactile_depth[2,240,320]` 和两路视频。v3 共29条episode/6626帧，test2 next-state共39条episode/12126帧；两者第0条均已用本地 Python 环境只读加载。
- **根因或明确假设**：这不是数据损坏。原因是大张量使用 Python 容器表示时被完整展开，而 `image`、`wrist_image` 是由同episode视频按帧索引解码，不直接嵌在Parquet行中。
- **解决方案**：用 PyArrow 只读取目标episode和行，对大字段仅打印 shape、dtype、范围或前几个元素；图像按 `frame_index / fps` 从对应 MP4 抽帧。需要训练管线完全一致的解码结果时，再用 `lerobot.datasets.LeRobotDataset`，并为 Hugging Face cache 指定可写目录。
- **验证范围**：已只读验证两个数据集episode 0/frame 0的数值字段、任务文本及两路视频路径；没有执行全量数据审计，也没有改变训练、标签或原始文件。
- **面试讲法**：大型机器人样本是多模态索引，不应把“查看样本”等同于递归打印所有数组。先按字段摘要数值，再按同一episode/frame解码视频，既保留语义又避免调试输出本身成为故障。

## 34. 两卡白板全参数微调的显存与初始化契约

- **日期**：2026-09-16。
- **状态**：首次两卡AdamW真实运行在首个优化步骤OOM，未完成任何训练更新、未生成checkpoint，sent-command未启动。已新增保持FP32全参数的无状态SGD配置并通过抽象显存与CPU合约；新GPU首步尚未运行验证。
- **现象**：用户希望利用额外两张48 GiB A6000，在现有LoRA实验之外，用相同next-state与sent-command白板数据各训练一个全参数模型，并要求第一套成功后自动启动第二套。直接把模型variant从LoRA改成普通Gemma会与Tabero 49999参数树不一致；简单两卡数据并行又会在每卡复制完整参数和Adam状态，存在明显OOM风险。
- **证据**：Tabero 49999对应的模型结构本身包含`gemma_2b_lora`与`gemma_300m_lora`叶子，严格loader会拒绝普通Gemma结构。训练代码以`trainable_filter = Param AND NOT freeze_filter`决定优化范围，因此`freeze_filter=nnx.Nothing`可让基础权重、checkpoint内既有LoRA、视觉/语言、动作/力头和触觉TCN全部更新。两CPU设备的抽象FSDP sizing得到3,353,275,152个可训练参数；全局float32参数约12.492 GiB、Adam状态约24.984 GiB，`fsdp_devices=2`后每卡参数与优化器状态约18.763 GiB，尚未包含梯度、激活和编译临时buffer。当前输出盘约有873 GiB可用。
- **根因或明确假设**：配置风险来自“全量”与“去掉LoRA结构”被错误等同，以及FSDP和数据并行内存语义不同。保留LoRA结构并不代表只训练LoRA；真正决定训练范围的是freeze filter。抽象静态状态估算遗漏了编译后反向图、梯度和输出state同时驻留的峰值；真实XLA日志显示rematerialization后计算仍需37.53 GiB，随后每卡还要分配18.76 GiB，明确超过48 GiB容量。因此本次失败是显存不足，不是W&B、数据、网络或数值异常。
- **解决方案**：原AdamW配置保留用于失败provenance；新增两个collision-safe的`pi0_tabero_whiteboard_*_force_full_ft_sgd_12k`配置，继续严格恢复Tabero 49999、`freeze_filter=nnx.Nothing`、FP32主权重、完整触觉TCN和全部33.53亿参数，但把AdamW的全尺寸一/二阶矩状态替换为带global-norm clip的scheduled SGD且不使用momentum。优化器状态因此只剩4字节schedule计数器，两卡FSDP每卡持久state抽象值从约18.763 GiB降到6.254 GiB；LR改为500步warmup、`1e-5`余弦衰减到`1e-6`。直接改BF16主权重被拒绝，因为小学习率更新写回BF16可能被量化吞掉。新运行使用独立config/asset/checkpoint/W&B命名空间，不允许恢复无checkpoint的旧失败pair。
- **验证范围**：Bash语法、Python编译、格式/空白检查和新旧白板配置/启动器共26项CPU测试通过。真实AdamW运行完成两套normalization、严格加载Tabero 49999、两卡FSDP初始化、W&B run `htwauln8`上传和1000帧step-0验证；第一训练步在两卡均报`RESOURCE_EXHAUSTED`，所以没有任何参数更新或checkpoint。新SGD配置的两设备抽象state确认两模型均有3,353,275,152个可训练参数、优化器状态4字节、每卡state约6.254 GiB，启动器dry-run和输出隔离通过；尚未占用GPU或执行真实首步，因此仍不能保证XLA峰值必然低于48 GiB。没有sent-command、离线推理、shadow或真机验证。
- **面试讲法**：全参数微调是“哪些叶子进入优化器”的问题，不是模型名字里能否出现LoRA。为了复用带LoRA叶子的已发布checkpoint，我保持参数树不变但取消冻结，并用两卡FSDP切分参数和Adam状态；同时用小学习率与独立资产保证这次实验能和LoRA基线公平比较。

## 35. 全量SGD早期平台期、三卡AdamW分片与GPU驱动失联

- **日期**：2026-09-17。
- **状态**：SGD已真实完成至少990次有限更新，但在第一次step-1000周期验证前中止，尚不足以证明最终不收敛；三卡AdamW只完成抽象分片评估，未做GPU首步；同机LoRA sent-command也在step 4811附近中止但有完整4000 checkpoint，本次中止的GPU驱动根因未定。
- **现象**：两卡无动量SGD全参数训练的逐批loss在前约1000步强烈波动，用户怀疑优化器导致无法收敛，并希望LoRA结束释放第三张卡后改用AdamW。诊断期间日志停在step 990、训练进程消失，且`nvidia-smi`报告无法与NVIDIA驱动通信。
- **证据**：本地`metrics.jsonl`共有98条训练采样，step 1--200、201--400、401--600、601--800、801--1000的action-loss均值依次为1.80433、1.78132、1.77312、1.84836和1.75542，没有单调下降；平均梯度范数约14--15，而配置的global-norm clip为1.0，几乎每步均被强裁剪。`param_norm`在日志精度下保持1392.2089，但这只能说明相对更新很小，不能证明参数逐元素完全未变。唯一完整验证仍是step 0的`action_loss=1.869755`，step 1000验证未产生。三CPU设备的FSDP抽象估算显示AdamW全局参数12.492 GiB、优化器状态24.984 GiB；因257152×2048词嵌入和多个4096维矩阵不能按3整除，每卡持久参数加优化器状态为16.910 GiB，比理想均分多4.418 GiB。两卡AdamW此前已经在首步以37.53 GiB计算峰值加18.76 GiB状态分配OOM。全机无任何`train.py`进程；LoRA sent-command日志也在step 4811结束，但其4000 checkpoint完整。当前训练日志没有Python/CUDA traceback且驱动接口失联，因此不能把终止归因于loss或W&B。最新Codex执行环境中`nvidia-smi`仍失败且没有`/dev/nvidia*`节点，但内核模块已加载、`/proc/driver/nvidia/gpus`仍列出四张A6000；这只能证明当前执行上下文缺少可用设备接口，不能区分宿主驱动故障与容器/会话设备节点未映射。
- **根因或明确假设**：SGD平台期与无动量、缺少Adam的逐参数自适应缩放、`1e-5`小学习率和持续强裁剪一致，是有证据支持但尚未由对照实验确认的优化困难；小batch造成的批间噪声也会放大曲线波动。三卡数量与模型的大量2次幂维度不匹配会产生显著复制，是否首步OOM仍只能由真实GPU预检确认。本次进程中止最符合GPU驱动失联或整机GPU重置，但无内核日志权限和明确Xid记录，故根因未定。
- **解决方案**：先由管理员恢复NVIDIA驱动或重启主机并验证所有GPU，再从4000 checkpoint恢复LoRA sent-command；不把现有SGD曲线作为最终模型结论，也不从该运行切换优化器续训，SGD在4000前没有checkpoint。若做公平的三卡AdamW实验，应在GPU 0/1/2都可用且驱动恢复后，从同一Tabero 49999初始化新run，使用`fsdp_devices=3`和能被3整除的global batch（最小为3），先完成独立首步显存预检再决定长训。若目标是继续已训练LoRA权重，则必须建立保持其触觉LoRA参数树的新全量配置、仅加载参数并重新初始化Adam状态，不能直接恢复SGD或混用rank-0全量配置。更稳妥的内存方案是等待四卡FSDP，或实现低状态自适应优化器/选择性解冻；不能宣称三卡AdamW必然可运行。
- **验证范围**：已只读统计SGD前990步本地训练指标、检查进程、`nvidia-smi`、设备节点、内核模块及`/proc` GPU枚举，并以三CPU设备计算AdamW抽象分片；未取得宿主管理员侧设备节点/驱动重载证据，未停止任何运行、未启动新训练，也未执行GPU编译/首步、step-1000验证、离线推理、shadow或真机部署。
- **面试讲法**：我没有凭一条抖动曲线把问题归罪于SGD，而是先比较分段均值、验证指标、梯度裁剪比例和参数尺度；同时用真实参数维度验证三卡FSDP会因不可整除而复制大矩阵。最终把“优化可能学得慢”“三卡可能OOM”和“驱动失联导致进程终止”拆成三个不同结论，并为每个结论标明证据边界。

## 37. full-SGD sent-command 在step-0验证产生NaN

- **日期**：2026-09-17。
- **状态**：直接失败点已定位，数据与归一化的非有限值已排除；模型前向或GPU运行时层面的唯一根因尚未复现确认。
- **现象**：W&B run `full_ft_sgd_whiteboard_20260916_sent_command`（ID `88cd9jmz`）显示Failed。顺序启动器此前已完成next-state全量SGD的12000步与4000/8000/12000 checkpoint，随后正常启动sent-command，但sent-command没有产生训练指标或checkpoint。
- **证据**：sent-command日志明确在首次`evaluate(step=0)`后抛出`FloatingPointError: Non-finite validation metrics at step 0`，聚合结果为`action_loss=nan, loss=nan, num_batches=500, num_frames=1000`；失败发生在任何SGD更新之前，配置严格从Tabero 49999加载全部有限参数。pair状态为`failed/train_sent_command/exit_code=1`，sent-command目录只有`run_config.txt`和`wandb_id.txt`，没有`metrics.jsonl`或数字checkpoint。训练集normalization JSON全部有限；CPU用相同loader遍历完整500个验证batch/1000帧，所有图像、状态、触觉和动作张量均有限，归一化sent-command范围约`[-5.394,4.120]`。同口径next-state范围约`[-5.307,4.825]`，并不更小，且next-state已完成step-12000验证`action_loss=1.639652`。失败后宿主`nvidia-smi`无法连接驱动，但当前无Xid/内核日志可证明时间先后因果。
- **根因或明确假设**：可以排除W&B网络上传、checkpoint保存、SGD更新发散、原始loader直接输出NaN以及简单的目标绝对幅度过大。剩余主要假设是：（1）某个sent-command动作模式与固定验证噪声组合在BF16模型前向中触发非有限值；（2）长时间满载后GPU/驱动已进入异常状态，step-0 NaN是随后驱动失联的早期症状。二者目前都只是明确假设，不能把时间相关性写成已证实根因。
- **解决方案**：不要直接重启12k。先恢复驱动并确认`nvidia-smi`和短GPU算子稳定，再增加逐验证batch的finite检查，记录第一个坏batch索引、RNG、输入摘要以及`compute_loss`的输出分量；用同一Tabero 49999参数分别重放next-state与sent-command坏batch。若重启后同一batch确定性复现，继续在`x_t`、suffix embedding、LLM输出和action projection处分段检查；若无法复现且驱动再次失联，则转向管理员采集NVIDIA Xid/ECC/温度/电源日志。当前失败run没有checkpoint，不能使用`--resume`，修复后必须新建collision-safe实验名。
- **验证范围**：完成本地日志、状态、产物和配置只读核对；CPU遍历两种数据的完整验证loader并比较归一化范围。没有再次分配GPU、没有重放模型前向、没有修改训练代码或启动训练，也没有取得内核Xid日志，因此根因仍待验证。
- **面试讲法**：W&B的Failed只是结果，真正失败点是训练前的step-0验证NaN。我先利用执行顺序排除优化器，再全量遍历loader排除输入NaN，并用成功的next-state对照排除简单幅度解释；最后把模型BF16前向和GPU驱动异常保留为两个可复现检验的假设，而不是提前归因。

## 38. 同规模pi0.5双卡DDP仅33 GiB与本项目AdamW OOM差异

- **日期**：2026-09-17。
- **状态**：同门侧代码已确认大部分VLM/Action Expert参数为BF16、少量模块FP32且无额外FP32 master副本，33 GiB已有合理解释；Adam状态实际dtype、首次step后的准确统计仍待打印确认。
- **现象**：同门报告相近参数量的pi0.5使用双卡DDP、每卡batch 2、AdamW、峰值学习率`2e-5`衰减到`2e-6`，每卡只占约33 GiB；本项目Tabero全量AdamW即使用两卡FSDP、global batch 2仍在首步OOM。
- **证据**：截图中的AdamW `betas=(0.9,0.95)`、`eps=1e-8`与本项目一致，`weight_decay=0`和本项目`1e-10`在内存上没有实质差异。真实失败run使用`2e-6→2e-7`，但学习率数值不会改变参数或优化器状态大小。同门侧代码复核称大部分VLM与Action Expert参数为BF16，少量视觉嵌入、LayerNorm、动作投影和时间MLP为FP32，使用原生`torch.optim.AdamW`且没有显式FP32 master副本；这仍是全量微调而非冻结或LoRA。若约35亿参数的参数、梯度、一阶矩和二阶矩均按BF16估算，约为8字节/参数，即26.1 GiB，再加FP32小模块、激活、DDP bucket和临时张量，33 GiB量级合理。本项目`init_train_state`只把匹配`freeze_filter`的冻结参数转成BF16；全量配置使用`freeze_filter=nnx.Nothing`，因此全部3,353,275,152个可训练参数仍为FP32，模型配置中的`dtype='bfloat16'`只代表主要计算精度，不代表参数存储精度。单卡DDP理论上仅FP32参数约12.492 GiB、FP32梯度约12.492 GiB、Adam一二阶矩约24.984 GiB，合计约49.968 GiB，未计激活、通信bucket和临时buffer就已超过48 GiB。两卡FSDP将持久参数加Adam状态降到约18.763 GiB/卡，但真实XLA首步仍需要37.53 GiB计算峰值并尝试额外分配18.76 GiB而OOM。
- **根因或明确假设**：核心差异已明确为持久训练状态的混合dtype，而不是DDP或更高学习率。同门的大体内存解释成立；但原生AdamW的状态通常跟随参数dtype这一点仍应以首次`optimizer.step()`后逐dtype统计为准，且准确可训练参数数与33 GiB统计口径仍未取证。PyTorch DDP与JAX/XLA在in-place/fused更新、梯度bucket和输出train-state buffer生命周期上的差异也会影响峰值。
- **解决方案**：先向同门核对完成首个optimizer step后的`torch.cuda.max_memory_allocated()`、参数/梯度/Adam `exp_avg`与`exp_avg_sq`的dtype和device、准确trainable参数数、AMP/FP32 master weight、gradient checkpointing、fused/8-bit Adam、offload、图像/token/action horizon。若要复现33 GiB布局，应基于这些事实设计独立配置；不能只把本项目学习率改成`2e-5`。本项目可选方向是低精度优化器状态、低状态自适应优化器、参数/状态offload、选择性解冻或更多GPU；直接把FP32主参数改BF16虽省内存，但存在小更新写回时被BF16量化吞掉的风险，必须单独验证。
- **验证范围**：已检查截图、当前源码与真实失败run配置，并用实际参数数和dtype计算显存下限；没有读取同门代码、optimizer state或显存快照，未修改训练实现，未启动GPU实验。
- **面试讲法**：参数量相近不等于训练显存相近。训练峰值由参数、梯度、优化器状态、激活和框架buffer共同决定；这里用dtype逐项核算证明33 GiB不可能对应完整FP32 AdamW布局，从而把排查重点从学习率转向真实内存表示和测量时点。

## 37. 四套白板12000步权重缺少13D离线评估入口

- **日期**：2026-09-17。
- **状态**：动作与wrench离线评估实现、四套checkpoint盘点及CPU合同预检已完成；当前NVIDIA驱动不可用，尚未执行1001锚点的GPU模型推理。
- **现象**：用户要用验证episode `(1,7,17)`比较四套12000步白板权重，包括rank-32的sent-command/next-state、全量SGD next-state和rank-16 next-state。原`scripts/eval_tabero_offline.py`只接受7D action-only、触觉LoRA rank 16合同，既会拒绝rank 0/32架构，也不会验证或统计模型输出的6D wrist wrench。
- **证据**：实际磁盘上四个目标checkpoint均含已提交的`params/assets/train_state/_CHECKPOINT_METADATA`；相应配置均声明内部13D目标、输出`actions[50,7] + wrist_wrench[50,6]`。两套验证数据都选episode 1/7/17，长度为274/315/412，共1001个锚点。四次`--check-only`均通过首尾样本、finite、shape、episode索引和padding检查。原始`full_ft_whiteboard_20260916`在首步AdamW OOM且没有checkpoint；本次使用用户明确指定、真实完成12000步的`full_ft_sgd_whiteboard_20260916_next_state`。
- **根因或明确假设**：直接原因是旧评估器按早期7D action-only实验写死了模型合同与输出字段，白板训练后来扩展为13D动作+wrench目标但没有同步扩展离线推理指标。当前不能生成最终数值的直接阻塞是`nvidia-smi`无法与NVIDIA驱动通信；这不代表四个checkpoint损坏，也不能据此推断模型性能。
- **解决方案**：新增白板专用评估入口，严格绑定checkpoint本地normalization、对应的next-state或sent-command数据集、固定seed 42/10步denoise/stride 1以及验证episode 1/7/17；沿用位置毫米、SO(3)角度、夹爪毫米指标，并增加K系力L2（N）、力矩L2（N·m）、分量MAE、零wrench基线、逐horizon统计、每episode动作图和wrench图。批量脚本顺序加载四个模型以避免并发抢显存，结束后生成`comparison.csv/json`。sent-command只与自己的标签比较，报告中保留label-source provenance，避免把不同目标分布的绝对误差直接当成架构优劣。
- **验证范围**：Ruff、Python编译、Bash语法、新增wrench指标/产物测试2项以及白板配置相关26项测试通过；四个checkpoint共24个验证边界样本的CPU合同预检通过。完整旧离线测试文件另有5项既有失败，来源是测试启动时未把仓库根加入`PYTHONPATH`以及旧假样本使用全零触觉参考网格，与本次wrench实现无关。尚未加载GPU参数、产生模型预测、比较物理误差、做shadow或真机验证。
- **面试讲法**：多任务模型的离线评估必须跟随输出合同演进。我先盘点真实checkpoint，再让每个模型绑定自己的标签语义和checkpoint内归一化，用相同锚点与随机噪声评估7D动作和6D wrench；同时把sent-command与next-state的目标差异写进结果，避免将数据定义差异误判为模型能力差异。

## 39. 两卡FSDP混合精度全量AdamW策略

- **日期**：2026-09-17。
- **状态**：混合精度参数、FP32梯度和FP32 Adam状态主档，以及状态跟随参数的低显存备选档均已实现并通过CPU/抽象FSDP验证；用户报告师兄的相近布局已经真实跑通，但本项目尚未执行GPU编译和首个AdamW更新。
- **现象**：全FP32全参数AdamW在两张48 GiB A6000上首步OOM，而相近规模模型采用大矩阵BF16、小型敏感模块FP32时可在更低显存内训练。用户要求PaliGemma VLM、Action Expert及checkpoint已有LoRA叶子用BF16，norm、视觉输入embedding、state/action投影、time MLP、action输出层和触觉TCN用FP32，并保留未来切回全FP32的配置入口以及FSDP两卡训练方式。
- **证据**：新策略的真实抽象参数树共有3,353,275,152个可训练参数，其中3,283,869,040个为BF16，69,406,112个为FP32。FSDP=2下每卡参数约3.199 GiB。主档把整棵梯度和Adam一/二阶矩设为FP32：每卡梯度约6.267 GiB、Adam状态约12.508 GiB，参数+梯度+状态静态小计约21.975 GiB，尚未包含激活和XLA临时buffer。低显存备选让梯度/状态跟随参数dtype，静态小计约12.797 GiB。用户明确报告师兄已验证前一种布局可以运行，这是选择主档的外部实测依据；但不是当前JAX/XLA程序的首步证据。训练集有11,125个frame anchors，global batch 2时12,000 update约等于2.16次数据遍历，20 epoch约需111,250 update。
- **根因或明确假设**：原两卡OOM的已证实原因是所有参数本身也为FP32，加上FP32梯度/Adam状态和XLA首步峰值超过48 GiB；当前主档仍保留FP32梯度/状态，但把约32.84亿个大参数改为BF16，从而释放参数和相关编译输出空间。师兄跑通的结果提高了可行性，但PyTorch/JAX、DDP/FSDP和编译器buffer生命周期不同，所以本项目仍必须用真实首步验收。20 epoch对只有36条训练轨迹且相邻action chunk高度重叠的数据很可能增加过拟合或遗忘风险，不能把epoch数直接等同于性能。
- **解决方案**：扩展config-owned `ParameterDtypePolicy`，分别配置参数叶子、梯度和优化器状态。`mixed_bf16_adamw`主档将PaliGemma/Action Expert/已有LoRA大叶子设为BF16，将norm、视觉embedding、state/action/time投影和触觉TCN设为FP32，同时在JAX autodiff返回后、裁剪和Adam之前显式把全部梯度张量转为FP32，并用Optax包装器保证Adam一阶和二阶矩都以FP32初始化和更新；`optax.apply_updates`后参数仍按各自配置写回BF16或FP32。这里的“FP32梯度”准确指优化器接收到和持有的梯度张量为FP32；BF16参数叶子的原始cotangent由JAX先以BF16产生，不能把后转换误写成全反向算子均以FP32累积。`mixed_bf16_lowmem_adamw`保留梯度/状态跟随参数的备选。两档均使用`2e-5→2e-6`、FSDP=2、global batch 2、严格Tabero 49999初始化、W&B和每4000步checkpoint。另保留`fp32_adamw`和`fp32_sgd`。启动脚本用`TABERO_TARGET_STEPS`独立控制步数，避免再修改脚本常量。
- **验证范围**：混合参数路径、FP32梯度转换、两份Adam状态初始化及一次更新后的dtype、BF16参数写回、FSDP抽象分片和启动器profile均通过CPU合同；主档与低显存档的每卡静态大小已分别计算。没有启动本项目GPU训练，故不能宣称两张A6000一定不OOM，也未验证BF16权重的实际非零更新比例、验证loss、离线指标或真机性能。
- **面试讲法**：我把“混合精度”拆成参数存储、梯度存储、优化器状态和数值归约四层。依据已有同规模实测，让大矩阵参数保留BF16以节省显存，而梯度、Adam两份动量及关键小模块保持FP32，再用FSDP切分状态；同时保留低显存备选，并用逐dtype统计与当前框架的首步峰值作最终验收。

## 40. 顺序启动器预处理阶段尚无单模型训练日志

- **日期**：2026-09-17。
- **状态**：已定位；运行仍在预处理阶段，未发现训练失败证据。
- **现象**：启动混合精度pair后立即对`..._next_state.log`执行`tail -f`，返回“没有那个文件或目录”，容易被误解为tmux或训练启动失败。
- **证据**：pair状态文件显示`state=running`、`stage=prepare_sent_command`；已存在且持续增长的是`full_ft_mixed_fp32state_20k_20260917_pair.log`。next-state的norm stats和audit已经生成，sent-command审计正在逐episode输出；没有Traceback、Error或failed标记。启动脚本只在进入对应`train_*`阶段时才创建`$run_name.log`，因此预处理期间next-state单模型日志按设计不存在。
- **根因或明确假设**：直接原因是日志生命周期不同：pair总日志从启动器开始即创建，单模型日志由训练命令的`tee`延迟创建。该现象不是GPU OOM、W&B故障或训练进程退出。
- **解决方案**：预处理阶段跟踪pair总日志和`status.txt`；进入`train_next_state`后再跟踪单模型日志，或使用`tail --retry -F`等待文件出现。后续可考虑启动时预创建空的单模型日志，但本次没有为纯显示问题改变运行中的脚本。
- **验证范围**：只读检查pair状态、日志更新时间、audit/norm产物和错误关键字；当前Codex执行环境与用户tmux进程命名空间隔离，不能直接列出其tmux会话或进程，因此没有据此判断tmux内部PID状态。
- **面试讲法**：多阶段无人值守任务要区分编排日志和阶段日志。先查状态机再查对应日志生命周期，可以避免把“文件尚未创建”误判成训练失败。

## 当前推荐的端到端排查顺序

1. **验证数据不变量**：episode 边界、`actions[t]` 语义、时间戳、单位、shape、finite、视频/触觉同步。
2. **验证动作几何**：位置使用米，姿态用 SO(3) 相对旋转，夹爪区分单指和总宽。
3. **绑定模型资产**：checkpoint、config、norm stats、conversion 和 prompt 全部记录哈希与 provenance。
4. **离线成对评估**：固定 anchors、noise、seed、horizon、split，报告毫米、角度、P95/P99/max 和越界率。
5. **Shadow 分层记录**：完整 chunk、action0、action[k]、当前 state、观测年龄、传感器时间和 chunk 切换。
6. **检查分布偏移**：相机视角、TCP、起始六自由度姿态、触觉参考网格和任务文本。
7. **最后才做真机执行**：持续使能、workspace、target jump、tracking error、rate limit、底层 watchdog 和硬件停止全部有效。

## 面试回答模板

可以按下面六句话组织每个案例：

1. **场景和影响**：系统处在哪一阶段，问题阻塞了什么。
2. **可复现现象**：给出最小复现和关键数字。
3. **最初假设**：列出两到三个候选原因，不提前下结论。
4. **证据链**：说明如何逐层排除，并指出决定性证据。
5. **修复和保护措施**：既修根因，也增加断言、日志、哈希或 fail-closed 防护。
6. **验证边界**：说明什么已经被测试，什么还需要训练或真机实验。

后续每次出现新问题，都应追加一个带日期的独立小节，并同步更新“问题总览”和状态。
